import {lengths} from "./lengths";
import {powers} from "./power";
import {times} from "./times";
import {weights} from "./weight";

/** A list of all available units */
export const allUnits = [...lengths, ...weights, ...times, ...powers];
